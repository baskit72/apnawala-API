server.address=0.0.0.0
# Allows binding to all network interfaces
server.port=8080
spring.application.name=websitebuilder
# Database connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/webshopgroow
spring.datasource.username=root
spring.datasource.password=Ram@1622
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
# Automatically updates the schema based on entities
spring.jpa.show-sql=true
# Log the SQL statements executed

aws.s3.bucket-name=video-storage-ram
aws.s3.region=eu-north-1

spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB
